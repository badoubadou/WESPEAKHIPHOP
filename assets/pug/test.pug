doctype html
html
  head
    link(type="text/css" rel="stylesheet" href="css/test.css")


  body
    h1 The @font-face Rule
    div
      | With CSS, websites can finally use fonts other than the pre selected "web-safe" fonts.
    p
      b Note:
      |  Internet Explorer 8 and earlier, do not support the WOFF format (only supports EOT format).
